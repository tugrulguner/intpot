# Conversion: MCP (FastMCP) → CLI (Typer)
# Source:     ../mcp_server.py
# Command:    intpot to cli examples/mcp_server.py
#
# Original input:
#
#   from fastmcp import FastMCP
#   mcp = FastMCP("example-server")
#
#   @mcp.tool()
#   def add(a: int, b: int) -> int:
#       """Add two numbers together."""
#       return a + b
#
#   @mcp.tool()
#   def greet(name: str, greeting: str = "Hello") -> str:
#       """Greet someone by name."""
#       return f"{greeting}, {name}!"
#
# Generated output below:
# ─────────────────────────────────────────

"""CLI app generated by intpot."""

import typer

app = typer.Typer()



@app.command()
def add(
    a: int = typer.Argument(..., help=""),
    b: int = typer.Argument(..., help=""),
) -> None:
    """Add two numbers together."""
    # TODO: implement
    typer.echo("add called")


@app.command()
def greet(
    name: str = typer.Argument(..., help=""),
    greeting: str = typer.Option('Hello', help=""),
) -> None:
    """Greet someone by name."""
    # TODO: implement
    typer.echo("greet called")


if __name__ == "__main__":
    app()
