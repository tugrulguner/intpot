# Conversion: API (FastAPI) → MCP (FastMCP)
# Source:     ../api_app.py
# Command:    intpot to mcp examples/api_app.py
#
# Original input:
#
#   from fastapi import FastAPI
#   app = FastAPI()
#
#   @app.post("/add")
#   def add(a: int, b: int) -> dict:
#       """Add two numbers together."""
#       return {"result": a + b}
#
#   @app.post("/greet")
#   def greet(name: str, greeting: str = "Hello") -> dict:
#       """Greet someone by name."""
#       return {"message": f"{greeting}, {name}!"}
#
# Generated output below:
# ─────────────────────────────────────────

"""MCP server generated by intpot."""

from fastmcp import FastMCP

mcp = FastMCP("generated-server")



@mcp.tool()
def add(
    a: int,
    b: int,
) -> dict:
    """Add two numbers together."""
    # TODO: implement
    return "add called"


@mcp.tool()
def greet(
    name: str,
    greeting: str = 'Hello',
) -> dict:
    """Greet someone by name."""
    # TODO: implement
    return "greet called"


if __name__ == "__main__":
    mcp.run()
